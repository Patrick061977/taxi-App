<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- v6.40.0: Foreground-Service für laufende Schicht (GPS, Dispatch, etc.) -->
        <service
            android:name=".ShiftForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- v6.41.96: FCM-Service für Push-Notifications bei neuen Aufträgen -->
        <service
            android:name=".TaxiFCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- v6.41.99: Receiver für Annehmen/Ablehnen-Buttons in der FCM-Notification -->
        <receiver
            android:name=".RideActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="de.taxiheringsdorf.app.ACTION_ACCEPT_RIDE" />
                <action android:name="de.taxiheringsdorf.app.ACTION_REJECT_RIDE" />
            </intent-filter>
        </receiver>

        <!-- v6.41.96: Standard-Channel-ID + Icon für FCM-Notifications -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="taxi_heringsdorf_rides" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- v6.41.19: Hintergrund-GPS (Android 10+) für nativen GPS-Tracker im Foreground-Service -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- v6.40.0: Foreground-Service Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- v6.40.31: SMS-Versand über Fahrer-SIM (Tracking-Link an Kunden) -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- v6.41.95: Akku-Optimierung-Whitelist anfordern damit Samsung/Android die App
         im Hintergrund (Schicht aktiv) nicht killt. Nötig da Doze-Mode + One UI sonst
         den ShiftForegroundService nach 15-60 Min stoppen — Heartbeat-Timeout-Crashes. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
</manifest>
