node_modules/
functions/package-lock.json

# Strato-Upload wird generiert (nicht manuell editieren)
strato-upload/

# Capacitor — www/ ist Build-Output (kopiert aus Root)
www/

# Android Build-Artefakte
android/app/build/
android/.gradle/
*.apk

# Lock-Datei
package-lock.json
